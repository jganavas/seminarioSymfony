<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Mi paginilla{% endblock %}</title>
        <link rel="icon" href="{{ asset('styles/img/estrella.png') }}">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <header class="site-header">
            <div class="container">
                <div class="header-content">
                    <div class="logo">
                        <img src="{{ asset('styles/img/estrella.png') }}" alt="Logo">
                        <h1>Mi paginilla</h1>
                    </div>
                    <nav class="main-nav">
                        {% block nav %}
                            <ul>
                                <li><a href="{{ path('app_blog_index') }}">Inicio</a></li>
                                <li><a href="{{ path('app_blog_new') }}">Nuevo Post</a></li>
                                <li><a href="#">Acerca de</a></li>
                                <li><a href="#">Contacto</a></li>
                            </ul>
                        {% endblock %}
                    </nav>
                </div>
            </div>
        </header>

        <main class="main-content">
            <div class="container">
                {% block body %}
                    
                {% endblock %}
            </div>
        </main>

        <footer class="site-footer">
            <div class="container">
                {% block footer %}
                    <div class="footer-content">
                        <div class="footer-section">
                            <h3>Sobre Nosotros</h3>
                            <p>Un blog creado con Symfony para compartir ideas y conocimiento.</p>
                        </div>
                        <div class="footer-section">
                            <h3>Enlaces</h3>
                            <ul>
                                <li><a href="{{ path('app_blog_index') }}">Miau</a></li>
                                <li><a href="#">Política de Privacidad</a></li>
                                <li><a href="#">Términos de Uso</a></li>
                            </ul>
                        </div>
                        <div class="footer-section">
                            <h3>Contacto</h3>
                            <p>Email: info@miblog.com</p>
                            <p>Síguenos en redes sociales</p>
                        </div>
                    </div>
                    <div class="footer-bottom">
                        <p>&copy; {{ "now"|date("Y") }} Mi Blog. Todos los derechos reservados.</p>
                    </div>
                {% endblock %}
            </div>
        </footer>
    </body>
</html>
