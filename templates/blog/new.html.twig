{% extends 'base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
    <div class="page-header">
        <a href="{{ path('app_blog_index') }}" class="btn-back">← Back to list</a>
        <h1>Create New Post</h1>
    </div>

    <div class="form-card">
        {{ include('blog/_form.html.twig', {'button_label': 'Create Post'}) }}
    </div>
{% endblock %}
